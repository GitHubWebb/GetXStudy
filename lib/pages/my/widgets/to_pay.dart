import 'package:flutter/material.dart';

class ToPayWidget extends StatelessWidget {
  const ToPayWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
